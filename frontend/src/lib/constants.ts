/** Horizontal distance in px to commit a YES/NO swipe. */
export const SWIPE_THRESHOLD = 120;

/** Vertical distance in px (negative = upward) to commit a skip. */
export const SKIP_THRESHOLD = -100;

/** Max rotation in degrees at full horizontal swipe. */
export const ROTATION_FACTOR = 15;

/** Distance in px for the exit fly-off animation. */
export const EXIT_DISTANCE = 600;
