/** Width of a market card in the horizontal carousel. */
export const CARD_WIDTH_VW = 85;

/** Gap between cards in the carousel (px). */
export const CAROUSEL_GAP = 16;

/** Travel distance for the swipe-up pill (px). Set dynamically in component. */
export const PILL_TRAVEL_DEFAULT = 280;

/** Slippage threshold in percentage points. */
export const SLIPPAGE_THRESHOLD = 5;

/** Biometric step-up threshold in USD. */
export const BIOMETRIC_THRESHOLD = 100;

/** Consecutive losses before cooling-off prompt. */
export const COOLING_OFF_THRESHOLD = 5;

/** Default bet size in USD. */
export const DEFAULT_BET_SIZE = 10;

/** Min/max bet size in USD. */
export const MIN_BET_SIZE = 1;
export const MAX_BET_SIZE = 100;
